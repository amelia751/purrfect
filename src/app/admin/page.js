import AdminPage from '@/components/AdminPage';

export const metadata = {
  title: 'Admin · Purrfect Coffee',
  robots: { index: false, follow: false },
};

export default function Page() {
  return <AdminPage />;
}
